# PAT 시스템 구축 진행 상황 보고

## 세미나 개요
- **일시**: 2026년 9월 8일
- **주제**: Optotune FSM 기반 PAT 시스템 구축 현황 및 계획
- **발표자**: [연구실명]

---

## 1. 프로젝트 목표

### 시스템 구성
```
TX Laser → FSM (Optotune MR-E-3) → Beam Splitter
                                         ↓
                                    PSD (Newport CONEX) → Feedback Control
                                         ↓
                                    Photodiode (미사용)
```

### 최종 목표
- **PAA-EKF 기반 빔 추적**: 각속도 추정 및 예측 제어
- **고속 폐루프 제어**: FSM + PSD 피드백으로 빔 안정화
- **재현 가능한 시스템**: 코드 자동화 및 문서화

---

## 2. Day 1 작업 (2026-09-08 전반)

### 2.1 환경 구축

#### SDK 설치
- **optoKummenberg 1.0.5422** → **optoMDC 1.3.5434** 순서로 설치
- MR-E-3는 optoMDC ≥1.3.4781 필요
- PyPI 없음, Optotune Download Center에서 wheel 다운로드 필요

#### 문제 1: API 함수 오류
```python
# 오류 코드
self.mre = optoMDC.connect()  # ❌ AttributeError

# 해결
self.mre = optoMDC.connectmre3()  # ✅ MR-E-3 전용 함수
```

#### 문제 2: 객체 구조 오류
```python
# 오류 코드
self.mre.Mirror.Channel_0  # ❌ 'Mirror' 속성 없음

# 해결
self.mre.Channel_0  # ✅ 직접 접근
```

#### 문제 3: UTF-8 인코딩
```python
# Windows CP949 → UTF-8 강제 전환
if sys.platform == 'win32':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
```

### 2.2 COM 포트 매핑

| 장비 | COM 포트 | 확인 방법 |
|------|----------|-----------|
| PSD (Newport CONEX) | COM5 | USB 제거 후 장치 관리자 |
| FSM (Optotune MR-E-3) | COM6 | 동일 |
| TX Laser | COM7 | 동일 |

### 2.3 수동 빔 탐색 (fsm_jog.py)

#### 문제 4: 키보드 방향 반전
- **증상**: 위 화살표 → FSM 아래로 기울어짐
- **해결**: Y축 부호 반전
```python
elif k == "up":
    self.uy -= self.step  # 기존: += → 수정: -=
```

#### 결과
- **빔 위치 발견**: u=(+0.01150, +0.01600), power=51.00
- **문제점**: 재현성 낮음 (개방 루프 히스테리시스)

### 2.4 캘리브레이션 준비

#### 문제 5: 빔이 FSM 중심에 없음
```python
# fsm_calibrate.py에 CENTER_OFFSET 기능 추가
CENTER_OFFSET = (0.071, 0.028)  # fsm_jog.py에서 찾은 위치
# 캘리브레이션 시 이 위치를 중심으로 가진
```

---

## 3. Day 2 작업 (2026-09-08 후반)

### 3.1 자동 빔 탐색 시스템

#### 개발 동기
- 키보드 수동 조작 비효율적
- 빔 위치 재현성 문제
- 캘리브레이션 자동화 필요

#### 구현: fsm_find_beam.py
```bash
python fsm_find_beam.py              # 기본 (0.05 간격)
python fsm_find_beam.py --fine       # 정밀 (0.01 간격)
python fsm_find_beam.py --range 0.3  # 범위 확대
```

**알고리즘**:
1. FSM 범위 ±0.2를 그리드 스캔
2. 각 위치에서 PSD 파워 측정
3. 최대 파워 위치 자동 저장 (`beam_position.json`)

**결과 파일 구조**:
```json
{
  "fsm_u": [0.01150, 0.01600],
  "psd_mm": [-5.000, 2.800],
  "power": 51.00,
  "timestamp": "2026-09-08 14:32:15"
}
```

### 3.2 캘리브레이션 자동화

#### fsm_calibrate.py 개선
- **beam_position.json 자동 로드**
- 파일 없으면 수동 CENTER_OFFSET 사용
- 워크플로우 간소화

```bash
# 자동 워크플로우
python fsm_find_beam.py   # 1단계: 빔 찾기
python fsm_calibrate.py   # 2단계: 자동 캘리브레이션
```

**Jacobian 행렬 측정**:
- FSM unit 변화 → PSD mm 변위 관계
- 2×2 행렬로 축간 커플링 반영
- `fsm_calib.json` 저장

### 3.3 PID 제어 튜닝

#### 문제 6: 심각한 진동
- **증상**: 빔이 큰 폭으로 진동, 파워 급변
- **원인 분석**:
  1. **과도한 게인**: KI=1.5가 너무 높음 (적분항 windup)
  2. **미분 노이즈**: PSD 측정 노이즈 증폭
  3. **샘플링 불규칙**: dt 변동 → 적분/미분 왜곡

#### 튜닝 과정

| 파라미터 | 초기값 | 1차 수정 | 2차 수정 (현재) |
|----------|--------|----------|-----------------|
| KP | 0.35 | 0.08 | **0.03** |
| KI | 1.5 | 0.15 | **0.05** |
| KD | 0.002 | 0.001 | **0.0** (비활성화) |
| D_LPF_HZ | 40 | 20 | **10** |
| I_LIMIT | 0.6 | 0.4 | **0.3** |

**현재 상태**: 여전히 진동 발생 → 추가 분석 필요

---

## 4. 현재 시스템 상태

### 4.1 완료된 작업 ✅
- [x] optoMDC SDK 설치 및 API 수정
- [x] COM 포트 매핑 (PSD/FSM/Laser)
- [x] UTF-8 인코딩 해결
- [x] fsm_jog.py 방향 제어 수정
- [x] 자동 빔 탐색 시스템 구축
- [x] 캘리브레이션 자동화
- [x] GitHub 통합 및 문서화

### 4.2 진행 중 작업 🔄
- [ ] **PID 안정성 확보** ← 현재 최우선 과제
- [ ] Jacobian 캘리브레이션 검증
- [ ] 루프 주기 최적화

### 4.3 미해결 이슈 ⚠️

#### 이슈 #1: PID 진동
**증상**:
- 빔 위치 큰 폭 진동
- 파워 급변 (1 ↔ 100 범위)

**가설**:
1. **시간 지연 문제**: PSD 읽기 지연 (100-200ms?) → 위상 지연
2. **Jacobian 오류**: 잘못된 캘리브레이션 → 오차 계산 왜곡
3. **구조 진동**: FSM/PSD 기계적 공진

**다음 조치**:
- [ ] `fsm_calibrate.py` 실행하여 J 행렬 확인
- [ ] 루프 주기 측정 (실제 Hz 출력 확인)
- [ ] P 제어만 테스트 (KI=0, KD=0)

#### 이슈 #2: 빔 재현성
- 동일 FSM 위치에서도 파워 변동
- 광학계 정렬 불안정 가능성

---

## 5. 향후 계획

### 5.1 단기 목표 (이번 주)

#### Phase 1: 안정성 확보
1. **PID 진동 근본 원인 파악**
   - 루프 지연 분석
   - Jacobian 검증
   - 기계 공진 테스트

2. **게인 재튜닝**
   - Ziegler-Nichols 방법 적용
   - Step response 분석
   - 안정 범위 확보

3. **광학계 점검**
   - 빔 정렬 재확인
   - 기구 고정 상태 점검
   - PSD 응답 시간 측정

#### Phase 2: 캘리브레이션 완료
```bash
# 목표 워크플로우
python fsm_find_beam.py   # 빔 탐색
python fsm_calibrate.py   # Jacobian 측정
python fsm_PID.py         # 안정적인 추적 확인
```

### 5.2 중기 목표 (이번 달)

#### EKF 통합 준비
1. **각속도 추정기 구현** (paa_ekf.py)
   - 상태 벡터: [θ_x, θ_y, ω_x, ω_y]
   - 측정: PSD 위치 [x_mm, y_mm]
   - 예측 제어: 각속도 보상

2. **메인 루프 통합** (fsm_paa_ekf_main.py)
   - PID: 위치 오차 제어
   - EKF: 각속도 예측 및 피드포워드
   - 하이브리드 제어 구조

3. **성능 검증**
   - Step response 측정
   - Frequency response 분석
   - Disturbance rejection 테스트

### 5.3 장기 목표 (다음 달)

#### 시스템 확장
- [ ] Zaber 조동축 통합 (대 범위 획득)
- [ ] 외란 시뮬레이션 (플랫폼 흔들림)
- [ ] 다중 타겟 추적
- [ ] 실시간 데이터 로깅 및 분석

#### 문서화 및 재현성
- [ ] 설치 매뉴얼 완성
- [ ] 튜닝 가이드 작성
- [ ] 성능 벤치마크 정립

---

## 6. 기술적 인사이트

### 6.1 배운 점

#### Optotune SDK 특성
- PyPI 미제공 → 수동 설치 필수
- 버전별 API 차이 큼 (connect vs connectmre3)
- 문서 부족 → `dir()` 활용한 탐색 필요

#### FSM vs Zaber 차이
| 항목 | Zaber (기존) | FSM (신규) |
|------|--------------|------------|
| 제어 방식 | 개방 루프 (증분형) | 폐쇄 루프 (절대형) |
| 위치 피드백 | 필수 (되읽기) | 불필요 (내부 서보) |
| PID 구조 | P only | Full PID |
| 응답 속도 | 느림 (~Hz) | 빠름 (>100Hz 가능) |
| 튜닝 민감도 | 낮음 | **높음** ← 핵심 차이 |

#### 워크플로우 자동화 중요성
- 수동 작업 → 재현 불가
- JSON 기반 데이터 전달 → 파이프라인 구축
- GitHub 관리 → 변경 이력 추적

### 6.2 남은 질문

1. **PSD 응답 시간**: 실제 읽기 지연이 얼마인가?
2. **FSM 대역폭**: 100Hz 목표가 현실적인가?
3. **Jacobian 안정성**: 온도/시간에 따라 변하는가?
4. **EKF 필요성**: 단순 PID로 충분한가?

---

## 7. 리스크 관리

### 7.1 기술적 리스크

| 리스크 | 영향도 | 완화 전략 |
|--------|--------|-----------|
| PID 불안정 지속 | 🔴 High | 전문가 자문, 시뮬레이션 검증 |
| 캘리브레이션 실패 | 🟡 Medium | 수동 측정 병행, 단계별 검증 |
| 하드웨어 고장 | 🟡 Medium | 예비 부품 확보, 정기 점검 |
| 재현성 부족 | 🟢 Low | 문서화 강화, 자동화 |

### 7.2 일정 리스크

| 마일스톤 | 목표일 | 현재 상태 | 비고 |
|----------|--------|-----------|------|
| PID 안정화 | D+3 | 🟡 지연 | 진동 문제 해결 중 |
| 캘리브레이션 완료 | D+5 | 🟢 정상 | 자동화 완료 |
| EKF 통합 | D+14 | 🔵 예정 | PID 안정화 후 시작 |
| 최종 검증 | D+30 | 🔵 예정 | - |

---

## 8. 결론

### 8.1 성과 요약
✅ **환경 구축 완료**: SDK, 포트, 인코딩 모두 해결
✅ **자동화 달성**: 빔 탐색 → 캘리브레이션 파이프라인
✅ **문서화 완료**: GitHub + 작업 일지
⚠️ **PID 튜닝 진행 중**: 안정성 확보 필요

### 8.2 다음 단계 (우선순위)
1. **PID 진동 원인 규명** (최우선)
2. Jacobian 캘리브레이션 검증
3. 안정적인 추적 성능 확보
4. EKF 통합 준비

### 8.3 협조 요청
- 광학계 정렬 전문가 리뷰
- PSD 사양서 재확인 (응답 시간)
- 제어 이론 자문 (진동 분석)

---

## 부록: 주요 파일 구조

```
PAT/
├── pat_ws/
│   ├── fsm_optotune.py       # FSM 드라이버
│   ├── psd_conex.py          # PSD 드라이버
│   ├── fsm_jog.py            # 수동 빔 탐색 (키보드)
│   ├── fsm_find_beam.py      # 자동 빔 탐색 (NEW)
│   ├── fsm_calibrate.py      # Jacobian 캘리브레이션
│   ├── fsm_PID.py            # 폐루프 추적
│   ├── paa_ekf.py            # EKF 예측기 (미완)
│   ├── fsm_paa_ekf_main.py   # 통합 메인 (미완)
│   ├── beam_position.json    # 자동 생성
│   └── fsm_calib.json        # 자동 생성
├── SETUP_LOG.md              # 설치 과정 전체
├── 작업일지_2026-09-08.md   # Day 1 일지
└── 세미나_PAT_진행상황_2026-09-08.md  # 이 문서
```

---

**발표 준비 완료**
질문이 있으시면 편하게 말씀해주세요!
