# PAT 시스템 작업 일지

## 2026-09-08 (Day 1)

### 작업 목표
Optotune MR-E-3 FSM 및 Newport CONEX-PSD 환경 설정 및 초기 테스트

---

## 오전: 환경 설정

### 1. 초기 문제 발견
```bash
python check_env.py
```
**결과**: `ModuleNotFoundError: No module named 'optoMDC'`

### 2. optoMDC SDK 설치
- **출처**: https://www.optotune.com/software-download/
- **설치 순서**:
  ```bash
  pip install optoKummenberg-1.0.5422-py3-none-any.whl
  pip install optoMDC-1.3.5434-py3-none-any.whl
  ```
- **결과**: ✅ 설치 성공

### 3. 코드 수정 - API 호환성

#### 문제 1: `optoMDC.connect()` 함수 없음
**파일**: `fsm_optotune.py:70-72`

**수정 전**:
```python
self.mre = optoMDC.connect()
```

**수정 후**:
```python
self.mre = optoMDC.connectmre3()  # MR-E-3 전용 함수
```

**원인**: optoMDC 1.3.x는 장비별로 다른 함수명 사용

---

#### 문제 2: `MRE3Board` 객체 구조
**파일**: `fsm_optotune.py:75-76`

**수정 전**:
```python
for ch in (self.mre.Mirror.Channel_0, self.mre.Mirror.Channel_1):
```

**수정 후**:
```python
for ch in (self.mre.Channel_0, self.mre.Channel_1):
```

**원인**: `MRE3Board`에 `Mirror` 속성 없음

---

### 4. UTF-8 인코딩 문제 해결

#### 문제: Windows 터미널 CP949 에러
**파일**: `fsm_jog.py`

**에러**:
```
UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'
```

**해결**: `fsm_jog.py:36-39` 추가
```python
import sys
import io

if sys.platform == 'win32':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')
    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8', errors='replace')
```

---

## 오후: COM 포트 매핑

### 5. 초기 COM 포트 혼란
- 4개의 COM 포트 감지
- PSD와 FSM이 같은 COM6 사용 시도 → 충돌

### 6. 물리적 확인 (장치 관리자)
**방법**: USB 케이블을 하나씩 제거/추가하며 확인

| 장비 | COM 포트 | 하드웨어 ID | 설정 파일 |
|------|----------|-------------|-----------|
| TX Laser | COM7 | FTDI (VID:0403:6015) | - |
| FSM (MR-E-3) | COM6 | STMicro (VID:0483:A31E) | `fsm_optotune.py:29` |
| PSD (CONEX) | COM5 | FTDI (VID:0403:6001) | `psd_conex.py:16` |

**수정 파일**:
- `psd_conex.py`: `DEFAULT_PORT = "COM5"`
- `fsm_optotune.py`: `DEFAULT_PORT = "COM6"`

---

## 저녁: 연결 테스트 및 조그

### 7. 연결 테스트

#### PSD 테스트
```bash
python -c "from psd_conex import PSD; psd = PSD().open(); print('PSD OK'); psd.close()"
```
**결과**: ✅ `[PSD] connected on COM5`

#### FSM 테스트
```bash
python -c "from fsm_optotune import OptotuneFSM; fsm = OptotuneFSM().connect(); print('FSM OK'); fsm.close()"
```
**결과**: ✅ `[FSM] MR-E-3 connected, closed-loop XY mode, centered`

---

### 8. fsm_jog.py 실행

#### 첫 번째 정렬 시도
```bash
python fsm_jog.py
```

**GUI 조작**:
- 키보드 화살표로 FSM 제어
- 빔 위치 탐색

**성공 케이스**:
- **FSM 위치**: `u = (+0.071, +0.028)`
- **PSD 읽기**: `(+0.198, -0.085) mm`, power = 15.00 ✅

#### 재현성 테스트
1. `0` 키로 중앙 복귀 (`u = 0, 0`)
2. 다시 `u = (+0.071, +0.028)`로 이동
3. **결과**: PSD `(-5, -5) mm`, power = 0 ❌

**문제점 발견**:
- 개루프 제어의 재현성 부족
- FSM 히스테리시스 또는 드리프트
- 폐루프 제어 필요성 확인

---

## 캘리브레이션 시도

### 9. fsm_calibrate.py 수정

#### 문제: FSM 중앙에서 빔 없음
**오류**:
```
center: PSD = (-5.0000, -5.0000) mm, power = 0.00
RuntimeError: DELTA_MAX 까지 키워도 PSD 변위가 없다.
```

#### 해결책: 오프셋 기능 추가
**파일**: `fsm_calibrate.py:46`

```python
# FSM 중앙에서 빔이 없으면 fsm_jog.py에서 찾은 위치를 입력
CENTER_OFFSET = (0.071, 0.028)  # fsm_jog.py에서 찾은 위치
```

**수정 내용**:
- `autorange()` 함수에 `center_u` 파라미터 추가
- 모든 측정이 오프셋 위치 기준으로 동작
- 중앙차분법 유지 (오차 상쇄)

---

### 10. 캘리브레이션 재시도 (진행 중)

#### 첫 번째 시도
```bash
python fsm_calibrate.py
```

**결과**:
```
center: PSD = (-5.0000, -5.0000) mm, power = 0.00
  (오프셋 적용: FSM u = (+0.07100, +0.02800))
경고: 광량이 너무 낮다.

자동 레인징:
  [1] d=0.00200 -> 최대 변위 0.000 mm
  [2] d=0.02000 -> 최대 변위 6.148 mm  (범위 초과)
  [3] d=0.00813 -> 최대 변위 0.000 mm
  [4] d=0.08133 -> 최대 변위 0.000 mm
```

**문제**: 오프셋 위치에서도 빔 감지 실패

**가능한 원인**:
1. 레이저가 꺼짐
2. fsm_jog.py 이후 하드웨어 상태 변화
3. 오프셋 값이 부정확 (시간 경과로 드리프트)

**다음 조치**: fsm_jog.py로 현재 빔 위치 재확인 필요

---

## 추가 작업

### 11. GitHub 저장소 관리

#### Git 초기화 및 푸시
```bash
git init
git add .
git commit -m "Initial setup: optoMDC installation and FSM/PSD integration"
git remote add origin https://github.com/KoshCocna/PAT.git
git push -u origin main
```

**이슈**: 중복 파일 구조
- 루트와 `pat_ws/` 폴더에 파일 중복
- 루트의 중복 파일 제거

#### 문서 작성
1. **SETUP_LOG.md**: 전체 설정 과정 상세 기록
2. **세미나_PAT_시스템_설정.md**: 발표용 자료 (17 슬라이드)

---

## 현재 상태

### ✅ 완료
- [x] optoMDC SDK 설치
- [x] fsm_optotune.py API 수정
- [x] COM 포트 매핑
- [x] PSD/FSM 연결 확인
- [x] fsm_jog.py 실행 성공
- [x] UTF-8 인코딩 문제 해결
- [x] GitHub 저장소 설정
- [x] fsm_calibrate.py 오프셋 기능 추가

### ⚠️ 진행 중
- [ ] Jacobian 캘리브레이션 완료
- [ ] 빔 위치 안정화
- [ ] 재현성 문제 해결

### 🔴 차단 이슈
- **빔 위치 불안정**: fsm_jog.py에서 찾은 위치가 유지되지 않음
- **캘리브레이션 불가**: 안정적인 빔 위치 확보 필요

---

## 다음 단계

### 즉시 (Day 2)
1. **레이저 전원 확인**
2. **fsm_jog.py 재실행**하여 현재 빔 위치 확인
3. 빔을 찾으면 **즉시 캘리브레이션 실행** (빔이 움직이기 전)
4. 가능하면 물리적으로 빔을 FSM 중앙 근처로 정렬

### 중기 (Week 1)
1. Jacobian 캘리브레이션 완료
2. `fsm_PID.py` 폐루프 제어 테스트
3. PID 게인 튜닝
4. 장시간 안정성 테스트

### 장기 (Month 1)
1. PAA-EKF 획득 알고리즘 테스트
2. 폐루프 추적 성능 검증
3. 논문/발표 준비

---

## 배운 점

### 기술적 교훈
1. **PyPI 외부 패키지**: 공식 웹사이트 확인, 의존성 순서 중요
2. **COM 포트 디버깅**: 물리적 제거/추가가 가장 확실
3. **API 버전 차이**: 문서와 실제가 다를 수 있음, `dir()` 활용
4. **Windows 인코딩**: UTF-8 설정 필수

### 프로젝트 관리
1. **점진적 검증**: 각 단계별 테스트 중요 (fsm_jog → calibrate → PID)
2. **문서화**: 실시간 작업 일지가 디버깅에 도움
3. **재현성**: 하드웨어의 재현성 문제는 소프트웨어(폐루프)로 해결

---

**작성자**: Claude Code
**작업 시간**: 약 6시간
**다음 업데이트**: 캘리브레이션 성공 시
